/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { TransactionCategoryPipe } from './transactionCategory.pipe';

describe('Pipe: TransactionCategorye', () => {
  it('create an instance', () => {
    let pipe = new TransactionCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
