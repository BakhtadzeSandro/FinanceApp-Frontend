/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { TransactionsImagePipe } from './transactions-image.pipe';

describe('Pipe: TransactionsImagee', () => {
  it('create an instance', () => {
    let pipe = new TransactionsImagePipe();
    expect(pipe).toBeTruthy();
  });
});
